# Handover -- 2026-07-29

Branch: `feat/jupedsim-integration`. Internal note. Continues `handover_2026-07-20.md`.

## Headline

**The learned-model bridge works end to end.** `policy_r0800.onnx` (89D
nogoaldir + navmesh + temporal) drives JuPedSim 2.0 agents around the exact
L-corner from jupedsim#1625: all agents exit via the vertical corridor, none
leave the walkable area (`tests/test_e2e_jupedsim_trained_policy.py`). The
2026-07-20 baseline for that scenario was 0/20 exited, all pinned at y=2.0.

## Upstream state (jupedsim)

- Issue #1625 (`destination` not exposed to Python) is closed; **PR #1626
  merged 2026-07-21** and went further than asked: `target` -> `final_target`,
  `destination` -> `next_target`, both bound to Python. Upstream explicitly
  flags `next_target`-as-location as **temporary** -- likely to become an
  *orientation* toward the next target. Our adapter only derives a direction
  from it, so the change is absorbable, but re-run the overlay suite on every
  jupedsim pull.
- **PR #1627** replaced the custom-model callback arguments:
  `compute_next_state(dt, ped, geometry, neighborhood_search)` ->
  `compute_next_state(dt, ped, env_query)`. `EnvironmentQuery` offers
  `other_agents_in_range` (self-excluding, optional predicate),
  `line_segments_in_range`, `line_segments_in_grid_cell_distance`,
  `no_wall_between`, `inside_geometry`, `intersects_any` -- a richer WorldState
  source than the old pair.
- No released wheel contains any of this: latest release is v1.4.2 (May 2026),
  the custom-model layer lives on master only (2.0.0-dev). Local source build
  required.

### Local test environment

`/home/fabi/dev/jupedsim`: `upstream` remote (PedestrianDynamics) added,
master fast-forwarded to `49e3ddebd` (clean ancestor; the old `origin` is
chraibi's fork). Submodules synced. Out-of-source **`build-py312/`** (Ninja,
Release) is bound to CrowdRL's uv venv Python 3.12; the pre-existing
in-source cp310 build for the mamba env is untouched. Adapter tests:

    PYTHONPATH="/home/fabi/dev/jupedsim/python_modules/jupedsim:/home/fabi/dev/jupedsim/build-py312/lib" \
        uv run pytest packages/crowdrl-jupedsim/

Rebuild after pulling upstream: `ninja -C /home/fabi/dev/jupedsim/build-py312`.
The default suite skips all jupedsim modules (importorskip). Now that the
adapter is green, the pyproject-documented `.pth` wiring is safe to adopt if
we want these tests in the default run (uv venv recreation wipes it).

## Issue #7 -- implemented (self-describing ONNX)

Design decision after reviewing the config-contract WIP: **two serialization
schemes on purpose.**

- `config_resolved.yaml` stays the human training/eval schema
  (deg-denominated, parsed by `train_mappo.build_env_config`). It is LOSSY:
  it cannot express `RaycastConfig` (n_rays/fov/max_range/two_channel),
  `k_neighbours`, or `head_limit` -- fine while all runs use default rays/K,
  but never a deployment payload. Follow-up candidate: a guard in
  `cfg_dict_from_env_config` that raises when non-defaults would be dropped.
- `crowdrl_core.config_io` is the new lossless boundary (asdict/JSON, raw
  radians, nested raycast). Missing keys fill from defaults (new fields are
  default-off and post-date the artefact); unknown keys raise (the run used a
  feature this core cannot reproduce). The `crowdrl.*` metadata key names
  live here; exporter and reader both import them.

Write side (`crowdrl_train.export.export_onnx`): optional
`obs_config`/`action_config` (both or neither) + `provenance` dict ->
`metadata_props` (`crowdrl.schema_version` = "1", obs/action config JSON,
obs/action dims, provenance). Refuses to export when `obs_config.obs_dim`
contradicts the actor input width. Wired in `train_mappo.py` (run id + git
rev provenance) and the notebook 06 export cell.

Read side (`crowdrl_jupedsim.policy`): `OnnxPolicy.metadata`
(`PolicyMetadata`) parsed at load with cross-checks (corrupt/partial record,
unknown schema version, recorded obs_dim vs graph width, reconstructed vs
recorded width). `resolve_configs` decision table: metadata-only ->
self-configure; metadata + explicit -> field-for-field agreement or raise
(kills the nogoaldir same-width landmine); legacy artefact -> explicit
configs required + "unverified" warning; `ConstantPolicy` silent. The
resolved ObsConfig must derive the policy's actual input width -- handover
2026-07-20 finding 5 (silent `ObsConfig()` default saturating goal-dir at
+/-10) is closed. `LearnedPolicyModel` no longer defaults configs.

The package `__init__` imports the model classes lazily (PEP 562) so the
jupedsim-free surface (`OnnxPolicy`, `resolve_configs`) imports in CI; the
end-to-end contract test (`tests/test_config_metadata_roundtrip.py`, real
torch export -> deployment reader) runs there.

Legacy artefacts (`policy_r0400/r0800.onnx` etc.) deploy via explicit
configs rebuilt from `config_resolved.yaml` and warn. Parked: re-export the
best checkpoint with embedded metadata and ship it in the repo.

## Adapter on jupedsim 2.0

- `4c09c25` port: `final_target`/`next_target` + `EnvironmentQuery`
  (containment via `inside_geometry`; the cached shapely polygon path is
  gone). Plan v8 3.2/3.6 passages corrected (the stale `ped.target` claims).
- `f04aa37` navigation: `WorldState.route_next_waypoints` -- an external
  router's per-agent next waypoint. Both obs builders prefer it over a
  navmesh query; `path_deviation` = 0.0 under the single-waypoint contract
  (bounded, in-distribution: normalizes to -0.37 sigma per the baked stats;
  the behavioural reliance on rare p_dev spikes remains an open ablation).
  Also fixed: `use_navmesh=True` with neither source silently DROPPED the 3
  dims (89D config -> 86D obs); a route now fills them.
- `dfae80a` temporal memory: `TemporalMemory` on `CrowdRLAgentState`,
  replicating CrowdEnv exactly (slots pre-filled with spawn values;
  post-step, post-containment position written at the PRE-step count's slot;
  count incremented after). Lazy init on first `compute_next_state` -- spawn
  and routed goal are unknowable at `add_agent` time. Buffers are
  copy-on-write; the state object is shared live with the simulation.

### Known caveats (documented in code comments)

1. Neighbour temporal rows in `build_world_state` are indexed with the EGO's
   step count; buffers of agents added at different times would misread.
   Harmless until `use_neighbor_trajectory_features` (A++) is wired --
   per-agent read indices needed then.
2. Contact forces remain absent (walking-skeleton scope): agents can
   interpenetrate; JuPedSim contributes no collision resolution.
3. `check_model_constraint` is not overridden; JuPedSim's default constraint
   throwing still applies.

## Consumer-site map (config contract)

Producers: `train_mappo.py` (writes `config_resolved.yaml` at run start),
notebook 06 (export cell writes the config/onnx/checkpoint triple). Readers:
`scripts/eval_scorecard.py`, `scripts/render_cpu.py`, notebook 08,
`tests/test_train_mappo.py`. Hardcode still to migrate:
`scripts/diagnose_stuck_agents.py` builds its own ObsConfig/ActionConfig.
Notebook 08 imports `train_mappo` via sys.path -- acceptable on the training
side; deployment no longer needs that mapping at all.

## Done later the same day

- **r0400 re-export shipped.** `scripts/reexport_onnx.py` rebuilds actor +
  frozen normalizer from a checkpoint + `config_resolved.yaml` and exports
  with embedded metadata. `example_model/policy_r0400.onnx` (gitignore
  exception added) is **bit-exact** vs. the run's original artefact
  (max |diff| 0.000e+00 on 256 random inputs) and self-configures:
  `LearnedPolicyModel(OnnxPolicy(path))`, nothing else.
- **Bottleneck scenario: crowd behaviour translates, with one caveat.**
  12 agents through a 1.4 m aperture (self-configured r0400): 12/12 exit in
  12.0 s sim (first 4.7 s, median 6.5 s, last 12.0 s; specific flow ~1.2
  (m s)^-1, near the empirical bottleneck range). The corner also passes
  with r0400 fully self-configured (4/4, 12.1 s). **Caveat:** min pairwise
  distance in the neck hit **0.044 m** -- near-total interpenetration for a
  moment. Navigation and throughput translate; spacing discipline in the
  squeeze does not, because contact forces are not wired in the adapter
  (caveat 2 above). Wiring the shared crowdrl-core contact-force module into
  the adapter integration is the natural next physics step; the e2e test
  documents but deliberately does not assert on spacing until then.
  All three scenarios live in `tests/test_e2e_jupedsim_trained_policy.py`;
  the self-configured ones need no environment variables.

- **Contact physics wired (Option A).** User observed agents' centres
  reaching the walls (zero-radius behaviour) in the notebook animation --
  correctly diagnosed as OUR walking-skeleton gap, not jupedsim (which never
  resolves collisions for operational models). `LearnedPolicyModel` now takes
  `walkable_geometry` (the same shapely Polygon given to `jps.Simulation`)
  and, when provided, runs the training env's exact physics per step, in the
  exact crowd_env.step order: contact accelerations (crowdrl-core
  `compute_contact_forces`, stiffness 30000 / damping 500 defaults) on
  pre-integration positions with post-filter velocities -> `v += a*dt` ->
  magnitude clamp -> integration -> `enforce_wall_boundaries` (body clearance
  = max(shoulder, chest) = 0.225 m default). Inherent approximation of the
  per-agent callback contract: neighbours are one step stale for the ego's
  force computation. Without the argument the old centre-point containment
  runs and construction WARNS. Results: wall clearance holds (>= 0.225 m
  everywhere, e2e-pinned); bottleneck min pairwise distance 0.289 m (was
  0.044 m -- head-on body contact is 0.30 m); 12/12 still exit but in ~30 s
  (was ~12 s of ghosting): a real queue forms, and the resulting throughput
  sits BELOW the empirical bottleneck range -- a policy-behaviour finding
  (yielding / dense packing, consistent with the known high-density
  weaknesses), not an adapter gap. Follow-up candidate: contact/dynamics
  params (stiffness, damping, desired_velocity_weight, max_velocity_magnitude)
  are env-level config that does NOT travel in the #7 metadata (schema v1
  covers obs+action only) -- consider a schema v2 embedding the dynamics
  block so deployment cannot drift on physics constants either.

- **Behavioural parity test (notebook 10, final section).** Same corner
  geometry, same spawns, same policy: JuPedSim deployment vs a faithful
  CrowdRL-native re-implementation of CrowdEnv.step (navmesh waypoint + true
  p_dev; jupedsim's exit-polygon removal rule for comparability), both at the
  run's trained dynamics `desired_velocity_weight=0.8`. Results: route
  topology and the waypoint handoff transfer; 3/4 agents track with mean
  path deviation 1.4-1.7 m and exit within ~1 s (JuPedSim slightly earlier);
  1/4 diverges on the JuPedSim side -- enters the vertical corridor
  wall-pinned at exactly body clearance, then creeps BACKWARD (~1.9 m over
  28 s) with the waypoint pointing straight up: the checkpoint's known
  freeze/creep failure mode. Verified NOT an adapter bug: exited agents are
  invisible to observations on both sides (sensing honours active_mask), so
  remove-vs-mask lifecycle does not enter. At the adapter default w=0.05,
  4/4 exit on BOTH sides (jps 8.9-13.2 s vs native 8.8-11.1 s).
  Presentation mirrors getting_started: side-by-side pedpy.plot_trajectories
  panels + N-t overlay + per-agent deviation table. Reinforces schema v2: w
  and contact constants must travel with the artefact.

  **Root cause of the divergence -- corner-clearance mismatch in the
  waypoint source (user diagnosis, verified in the trace).** JuPedSim's
  router targets the sharp corner itself: the measured waypoint
  (10.14, 1.86) sits 0.20 m from the corner vertex (10, 2) -- INSIDE the
  0.225 m body radius. Training's funnel insets every portal endpoint by
  agent_radius (`navmesh.py::_inset_portal`, "keeps the agent centre at
  least agent_radius away"), so during training the router never lets
  agents run at corners/walls like that and the policy never learned to
  handle it: the lead agent aims at a point its body cannot occupy and
  crashes into the corner (the wall projection pins it), and agents behind
  then crash into the pinned leader -- a pile-up mechanism that will bite
  harder at crowd densities than in the 4-agent corner. p_dev=0 is
  secondary. Candidate fixes, in preference order: (a) adapter-side
  waypoint clearance -- push next_target off walls/corners to agent_radius
  before it enters the observation (restores the funnel's inset semantics
  at deployment, preserving train==deploy); (b) training-side
  waypoint-clearance randomization so future checkpoints tolerate
  corner-hugging waypoints; (c) upstream question to co-draft with the dev
  team: is radius-aware routing planned? (Built-in C++ models get the same
  corner-hugging waypoints but their hand-crafted wall repulsion
  compensates; a policy trained on inset waypoints cannot.)
  **(c) answered 2026-07-30: no.** The A* explicitly assumes point-size
  agents and the 0.2 m inset carries a TODO to be removed entirely, agent
  size is private to the operational model, and the router is handed only a
  position -- so radius-awareness is new upstream work, not a planned feature
  we can wait for. Evidence in `plan/lockstep_parity_analysis.md`, section
  "Why the router inset cannot be configured". (a) and (b) stand.

- **Byte-exact parity achieved (`LockstepPolicyModel`).** Full analysis in
  `plan/lockstep_parity_analysis.md`. Headlines: the waypoint source is the
  dominant divergence channel (funnel vs router: up to 116 deg direction,
  p_dev 0.29-0.46 vs 0.0 = +3..4 sigma); ORT batch-vs-single and the
  interpreters are exactly zero; JuPedSim's exit stage removes agents 2
  iterations late. The lockstep model runs the whole native batched step
  once per iteration (compute-then-apply reads pre-step state, so the first
  callback of a pass sees the native synchronous snapshot) and applies
  native removal semantics internally. Verified `np.array_equal`, no
  tolerances: corner 1082 steps + identical exit steps, bottleneck 400
  contact-heavy steps. `native_batch_step` is shared with the byte-parity
  test's reference loop, so identity is structural. Also settled en route:
  the freeze divergence at w=0.8 is a **policy absorbing state**
  (wall-facing, |v|~0, action = forward + zero turn; identical obs and
  action across engines at the captured state) -- a training target, not an
  adapter defect; the waypoint corner-clearance is committed as opt-in.

## Open items
- dt-validation upstream issue: deliberately resting until the adapter is
  end-to-end validated (user call, 2026-07-29). The e2e test pins dt=0.01.
- YAML schema gap guard (see issue #7 section).
- `plan/CrowdRL_Project_Plan_v5.docx` staleness question from the previous
  handover remains open.
- Local branch `memory_experiments` is 1 unpushed WIP commit ahead of origin
  (57ac229).
- **End-of-session deep review**: `final_review_2026-07-29.md` -- adversarial
  re-verification of everything above; 2 high-priority defects found
  (interactive heading anchoring, reexport dynamics provenance) + ordered
  next steps. Read it before building further on this branch.
